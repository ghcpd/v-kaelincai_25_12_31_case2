# Quick Start (fixed project)

1. Create venv: `python -m venv .venv` ; `.\.venv\Scripts\Activate.ps1`
2. Install: `pip install -r requirements.txt`
3. Run tests: `pytest tests/ -v`  # expects `16 passed`
4. Run app: `python -m src.app` and open http://localhost:5000
