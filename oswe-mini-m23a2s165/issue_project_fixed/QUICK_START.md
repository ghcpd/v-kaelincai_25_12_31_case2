# Quick Start Guide (fixed project)

1. Start the app:
   python -m src.app

2. Run tests:
   pytest tests/ -v

3. Open http://localhost:5000 and try different date formats — the backend now accepts common manual formats from Safari/Firefox as well as ISO from Chrome/Edge.
