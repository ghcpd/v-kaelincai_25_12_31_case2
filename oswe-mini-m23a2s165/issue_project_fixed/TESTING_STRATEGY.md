# Testing Strategy (fixed project)

This copy reflects that the previously-xfailed tests are now passing. The strategy and rationale remain the same: tests document expected behavior and xfail was used to track known failures during debugging.
